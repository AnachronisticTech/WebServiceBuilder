import XCTest
@testable import WebServiceBuilder

final class WebServiceBuilderTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(WebServiceBuilder().text, "Hello, World!")
    }
}
